[package]
name = "alios"
version = "0.1.0"
edition = "2024"

[build-dependencies]
kernel = { package = "alios-kernel", path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }
bootloader = "=0.11.12"

[dependencies]
heapless = "0.9.3"